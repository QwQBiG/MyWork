/*
注意：蓝桥杯环境（DevC++）
这玩意需要在上方菜单栏的 工具 找到 编译选项 在上方这个框框内写上
 -std=c11 并且勾选（不然for里面都不让定义变量）
*/

---

zbh是当时任课老师的名字，要求什么内容前面都加上一个，在我感觉没有什么意义。

---

zbh_Project/
├── main.c           // 程序入口，主循环
├── common.h         // 公共头文件：宏定义、结构体定义
├── globals.h        // 全局变量声明 (extern)
├── globals.c        // 全局变量定义
├── utils.h          // 工具函数声明
├── utils.c          // 工具函数实现 (对齐、高亮、辅助判断)
├── file_ops.h       // 文件操作声明
├── file_ops.c       // 文件操作实现 (读写、备份、导入导出)
├── logic.h          // 业务逻辑声明
├── logic.c          // 业务逻辑实现 (排序、计算、搜索、统计、增删改)
├── menus.h          // 菜单界面声明
└── menus.c          // 菜单界面实现 

---

功能展示流程:(如果忘了我就看看~) 
0) 8 -> 1。看看玩也行(硬编码)。 

1) 7 (数据维护) ->  5 (加载测试数据)。

2) 2 (浏览数据) :看看排名算法，以及排版外观(这时是用横线分开项目的) ，和中英文对齐。

3) 展示并列名次:两个选手成绩相同，系统自动判断为并列名次，并且下一个名次会自动顺延（例如 1, 1, 3）。

4) 3 (查询) -> 1 (按姓名/编号) -> 红色高亮字(ANSI 转义序列实现高亮)。 

5) 6 (比赛管理) -> 2 (成绩合理性验证)。 警告（如果有故意设置的异常数据）。

6) 4 (统计分析) -> 1 (学院排名榜)。

7) 不想写了，没有什么好玩的也。 

8) 对了，文件是有备份的。 (backup) 

---

还有，这玩意没有登录，我想不用也行，算是给后台人员自己统计用的；
那，这么说来没有多线程也是正确的；
直接上个数据库我觉得更好（）。 
